# Vendored Dependencies

The remaining vendored crates are patched through the workspace
`[patch.crates-io]` table. Historical YouTube and Reddit-only imports were
removed once their active dependency edges disappeared.
